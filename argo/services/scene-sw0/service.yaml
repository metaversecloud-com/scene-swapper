apiVersion: v1
kind: Service
metadata:
  name: scene-sw0
  labels:
    app.kubernetes.io/name: scene-sw0
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: scene-sw0
  ports:
    - name: http
      port: 80
      targetPort: http
